[phases.setup]
nixPkgs = ["python311", "chromium"]

[phases.install]
cmds = ["pip install playwright beautifulsoup4 requests schedule && playwright install chromium"]

[start]
cmd = "python scraper_v2.py"
